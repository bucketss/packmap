[package]
name = "packmap"
version = "0.2.0"
edition = "2021"

[[bin]]
name = "packmap"
path = "packmap.rs"

[dependencies]
time = { version = "0.3", features = ["formatting"] }
zip = "2"

[build-dependencies]
winresource = "0.1"
